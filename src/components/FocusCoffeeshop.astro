<section class="focus-sec" id="coffeeshop">
  <div class="container">
    <div class="focus-grid">
      <div class="focus-visual">
        <div class="f-mockup f-mockup-green">
          <div class="f-bar"><span></span><span></span><span></span><span class="f-bar-label">Prijslijst</span></div>
          <div class="f-menu-body">
            <div class="fm-row"><span></span><span></span></div>
            <div class="fm-row"><span></span><span></span></div>
            <div class="fm-row hl-g"><span></span><span></span></div>
            <div class="fm-row"><span></span><span></span></div>
            <div class="fm-row"><span></span><span></span></div>
          </div>
        </div>
      </div>
      <div class="focus-text">
        <span class="focus-tag">Specialisme #3</span>
        <h2>Coffeeshop signage — digitaal, professioneel, compliant</h2>
        <p>Speciaal voor coffeeshops biedt ClubDisplay digitale prijslijsten, productinformatie en menuborden die bijdragen aan een professionele en rustige uitstraling. Toon uw actuele aanbod op heldere schermen, beheer prijzen centraal en communiceer eenvoudig met bezoekers over regels, openingstijden en aanbiedingen.</p>
        <p>Onze narrowcasting software maakt het mogelijk om schermen op meerdere locaties centraal aan te sturen. Combineer productinformatie met sfeerbeelden, veiligheidsinstructies of actuele wachtrijinformatie. Alle content wordt professioneel vormgegeven en afgestemd op uw huisstijl.</p>
        <div class="focus-tags">
          <span>Digitale prijslijsten</span><span>Productinformatie</span><span>Centraal beheer</span>
          <span>Wachtrijcommunicatie</span><span>Compliant</span><span>Huisstijl</span>
        </div>
        <a href="/coffeeshop-signage/" class="btn">Ontdek coffeeshop signage <span aria-hidden="true">&rarr;</span></a>
      </div>
    </div>
  </div>
</section>

<style>
  .focus-sec { padding: 7.5rem 0; background: var(--bg-alt); }
  .focus-grid { display: grid; grid-template-columns: 1fr 1fr; align-items: center; gap: 4rem; }
  .focus-tag {
    display: inline-block; background: var(--primary-light); color: var(--primary-dark);
    padding: 0.25rem 0.875rem; border-radius: 999px; font-size: 0.75rem; font-weight: 600;
    text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 1.25rem;
  }
  .focus-text h2 {
    font-size: clamp(1.5rem, 2vw, 2rem); font-weight: 800;
    letter-spacing: -0.02em; line-height: 1.15; margin: 0 0 1.25rem;
  }
  .focus-text p { font-size: 0.938rem; color: var(--text-secondary); line-height: 1.75; margin: 0 0 1rem; }
  .focus-tags { display: flex; flex-wrap: wrap; gap: 0.5rem; margin: 1.5rem 0 2rem; }
  .focus-tags span {
    background: #fff; border: 1px solid var(--border);
    padding: 0.375rem 0.875rem; border-radius: 999px;
    font-size: 0.8125rem; color: var(--text-secondary); font-weight: 500;
  }

  .f-mockup { border-radius: var(--radius-xl); overflow: hidden; box-shadow: var(--shadow-lg); }
  .f-mockup-green { background: #064e3b; border: 1px solid #065f46; }
  .f-mockup-green .f-bar { background: #065f46; border-bottom-color: #047857; }
  .f-mockup-green .f-bar-label { color: #6ee7b7; }
  .f-bar {
    padding: 0.75rem 1rem; display: flex;
    gap: 0.375rem; align-items: center; border-bottom: 1px solid var(--border);
  }
  .f-bar span { width: 8px; height: 8px; border-radius: 50%; }
  .f-bar span:nth-child(1) { background: #ef4444; }
  .f-bar span:nth-child(2) { background: #f59e0b; }
  .f-bar span:nth-child(3) { background: #10b981; }
  .f-bar-label { font-size: 0.6875rem; font-weight: 600; color: var(--text-muted); margin-left: 0.5rem; text-transform: uppercase; letter-spacing: 0.04em; }
  .f-menu-body { padding: 1.25rem; display: flex; flex-direction: column; gap: 0.75rem; }
  .fm-row { display: flex; justify-content: space-between; gap: 1rem; }
  .fm-row span:first-child { height: 0.5rem; width: 60%; background: rgba(255,255,255,0.08); border-radius: 3px; }
  .fm-row span:last-child { height: 0.5rem; width: 20%; background: rgba(255,255,255,0.08); border-radius: 3px; }
  .fm-row.hl-g span { background: rgba(52,211,153,0.3) !important; }

  @media (max-width: 900px) {
    .focus-sec { padding: 5rem 0; }
    .focus-grid { grid-template-columns: 1fr; gap: 2.5rem; }
    .focus-text h2 { font-size: 1.5rem; }
  }
</style>
