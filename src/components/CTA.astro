<section class="cta" id="advies">
  <div class="container">
    <div class="cta-inner">
      <h2 class="cta-title">Advies nodig? Ontdek welke digital signage oplossing past bij uw locatie.</h2>
      <p class="cta-text">Plan een vrijblijvend adviesgesprek met een van onze specialisten. We bespreken uw wensen, doen een voorstel op maat en geven eerlijk advies — zonder verplichtingen.</p>
      <div class="cta-actions">
        <a href="/plan-jouw-gesprek/" class="btn btn-lg btn-white">Plan een adviesgesprek</a>
        <a href="tel:+31857602202" class="cta-phone">of bel +31 (0)85 760 2202</a>
      </div>
    </div>
  </div>
</section>

<style>
  .cta {
    padding: 6rem 0;
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
  }
  .cta-inner {
    max-width: 700px;
    margin: 0 auto;
    text-align: center;
  }
  .cta-title {
    color: #fff;
    font-size: 2rem;
    font-weight: 800;
    letter-spacing: -0.02em;
    line-height: 1.2;
    margin: 0 0 1rem;
  }
  .cta-text {
    color: rgba(255,255,255,0.85);
    font-size: 1.0625rem;
    margin: 0 0 2rem;
    line-height: 1.7;
  }
  .cta-actions {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.5rem;
    flex-wrap: wrap;
  }
  .cta-phone {
    color: rgba(255,255,255,0.85);
    font-weight: 600;
    font-size: 1.0625rem;
    text-decoration: none;
  }
  .cta-phone:hover { color: #fff; }

  @media (max-width: 768px) {
    .cta { padding: 3.5rem 0; }
    .cta-title { font-size: 1.5rem; }
    .cta-actions { flex-direction: column; }
    .cta-actions .btn { width: 100%; }
  }
</style>
