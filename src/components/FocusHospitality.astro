<section class="focus-sec" id="hospitality">
  <div class="container">
    <div class="focus-grid">
      <div class="focus-visual">
        <div class="f-mockup f-mockup-light">
          <div class="f-bar"><span></span><span></span><span></span><span class="f-bar-label">ClubDisplay Hospitality</span></div>
          <div class="f-body">
            <div class="f-block"></div>
            <div class="f-line"></div>
            <div class="f-line short"></div>
            <div class="f-grid-3">
              <span></span><span></span><span></span>
            </div>
          </div>
        </div>
      </div>
      <div class="focus-text">
        <span class="focus-tag">Specialisme #1</span>
        <h2>Hospitality TV & IPTV — specialist in gastbeleving</h2>
        <p>ClubDisplay is hét adres voor hospitality TV en IPTV-oplossingen. Als <strong>LG Pro:Centric partner</strong> leveren wij professionele hotel-tv systemen met interactieve welkomstberichten, PMS-koppelingen en naadloze Chromecast-integratie. Van boutique hotel tot internationaal resort — wij regelen het volledige traject.</p>
        <p>Onze IPTV-oplossingen zijn geschikt voor hotels, kantoren en zorginstellingen. Gasten genieten van een vertrouwde TV-ervaring met toegang tot Netflix, YouTube en eigen streamingdiensten, terwijl u als beheerder volledige controle heeft over content, zenders en noodmeldingen.</p>
        <div class="focus-tags">
          <span>LG Pro:Centric</span><span>IPTV hotel</span><span>Chromecast</span>
          <span>PMS-koppeling</span><span>Welkomstberichten</span><span>Zenderbeheer</span>
        </div>
        <a href="/zakelijke-iptv-voor-hotels-en-bedrijven/" class="btn">Ontdek hospitality & IPTV <span aria-hidden="true">&rarr;</span></a>
      </div>
    </div>
  </div>
</section>

<style>
  .focus-sec { padding: 7.5rem 0; background: var(--bg-alt); }
  .focus-grid { display: grid; grid-template-columns: 1fr 1fr; align-items: center; gap: 4rem; }
  .focus-text { }
  .focus-tag {
    display: inline-block; background: var(--primary-light); color: var(--primary-dark);
    padding: 0.25rem 0.875rem; border-radius: 999px; font-size: 0.75rem; font-weight: 600;
    text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 1.25rem;
  }
  .focus-text h2 {
    font-size: clamp(1.5rem, 2vw, 2rem); font-weight: 800;
    letter-spacing: -0.02em; line-height: 1.15; margin: 0 0 1.25rem;
  }
  .focus-text p { font-size: 0.938rem; color: var(--text-secondary); line-height: 1.75; margin: 0 0 1rem; }
  .focus-tags { display: flex; flex-wrap: wrap; gap: 0.5rem; margin: 1.5rem 0 2rem; }
  .focus-tags span {
    background: #fff; border: 1px solid var(--border);
    padding: 0.375rem 0.875rem; border-radius: 999px;
    font-size: 0.8125rem; color: var(--text-secondary); font-weight: 500;
  }

  .f-mockup { border-radius: var(--radius-xl); overflow: hidden; box-shadow: var(--shadow-lg); }
  .f-mockup-light { background: #fff; border: 1px solid var(--border); }
  .f-bar {
    background: var(--bg-alt); padding: 0.75rem 1rem; display: flex;
    gap: 0.375rem; align-items: center; border-bottom: 1px solid var(--border);
  }
  .f-bar span { width: 8px; height: 8px; border-radius: 50%; }
  .f-bar span:nth-child(1) { background: #ef4444; }
  .f-bar span:nth-child(2) { background: #f59e0b; }
  .f-bar span:nth-child(3) { background: #10b981; }
  .f-bar-label { font-size: 0.6875rem; font-weight: 600; color: var(--text-muted); margin-left: 0.5rem; text-transform: uppercase; letter-spacing: 0.04em; }
  .f-body { padding: 1.5rem; display: flex; flex-direction: column; gap: 0.75rem; }
  .f-block { height: 1rem; width: 55%; background: linear-gradient(90deg, var(--primary-light), #9fc4e7); border-radius: 4px; }
  .f-line { height: 0.5rem; width: 100%; background: var(--bg-alt); border-radius: 3px; }
  .f-line.short { width: 45%; }
  .f-grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 0.5rem; margin-top: 0.25rem; }
  .f-grid-3 span { height: 3.5rem; background: var(--bg-alt); border-radius: var(--radius-sm); }
  .f-grid-3 span:first-child { background: var(--primary-50); }

  @media (max-width: 900px) {
    .focus-sec { padding: 5rem 0; }
    .focus-grid { grid-template-columns: 1fr; gap: 2.5rem; }
    .focus-text h2 { font-size: 1.5rem; }
  }
</style>
